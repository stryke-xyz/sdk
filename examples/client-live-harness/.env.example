CHAIN_ID=4663
RPC_URL=https://rpc.mainnet.chain.robinhood.com

# Optional: defaults to the first AMM and first hook registered by the SDK.
AMM_ADDRESS=
HOOK_ADDRESS=
ADD_LIQUIDITY_ROUTER=

# Required raw token units for prepare and execution.
AMOUNT0_DESIRED=
AMOUNT1_DESIRED=

# Optional aligned tick bounds. Defaults to one tick spacing below/above spot.
TICK_LOWER=
TICK_UPPER=
DEADLINE_SECONDS=900

# Only read when you explicitly run with --execute. Keep this file local.
PVK=
